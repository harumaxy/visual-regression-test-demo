{
  "name": "visual-regression-test-demo",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "test": "bun run --filter '*' test",
    "build-storybook": "bun run --filter '*' build-storybook",
    "e2e": "bun run --filter '*' e2e",
    "vrt": "bun run --filter '*' vrt",
    "vrt:update": "bun run --filter '*' vrt:update"
  }
}
